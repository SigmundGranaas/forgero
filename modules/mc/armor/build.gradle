//file:noinspection GroovyAccessibility
//file:noinspection GroovyAssignabilityCheck
//file:noinspection DependencyNotationArgument

dependencies {
	implementation(project(":modules:core"))
	implementation(project(path: ":modules:mc:loader", configuration: 'namedElements'))
	implementation(project(path: ":modules:mc:common", configuration: 'namedElements'))
	implementation(project(path: ":modules:mc:properties", configuration: 'namedElements'))

	// Fabric
	modCompileOnly("net.fabricmc:fabric-loader:${project.loader_version}")
	modCompileOnly("net.fabricmc.fabric-api:fabric-api:${project.fabric_api_version}")

	// Test dependencies
	testImplementation("org.junit.jupiter:junit-jupiter-api:5.9.0")
	testRuntimeOnly("org.junit.jupiter:junit-jupiter-engine:5.9.0")
	modImplementation("net.fabricmc.fabric-api:fabric-api:${project.fabric_api_version}")
	modImplementation("net.fabricmc:fabric-loader:${project.loader_version}")
	testImplementation("net.fabricmc:fabric-loader-junit:${project.loader_version}")
	testImplementation(project(path: ":modules:mc:test-common", configuration: 'namedElements'))

	// Content data required for tests
	testRuntimeOnly(project(":content:forgero-materials"))
	testRuntimeOnly(project(":content:forgero-base"))
}

loom {
	runs {
		testServer {
			server()
			name = "Test Server"
			source sourceSets.test
		}
		gameTest {
			inherit testServer
			name "Game Test"
			vmArg "-Dfabric-api.gametest"
			runDir "build/gameTest"
		}
	}
}

sourceSets {
	test {
		compileClasspath += main.compileClasspath
		runtimeClasspath += main.runtimeClasspath
	}
}

test {
	useJUnitPlatform()
	maxParallelForks = 32
	testLogging.events("failed")
	testLogging.info.events = ["failed", "skipped"]
	// Exclude GameTests - they must run with runGameTest task
	exclude '**/ArmorItemTest.class'
}
