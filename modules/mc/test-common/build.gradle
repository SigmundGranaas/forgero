//file:noinspection GroovyAccessibility
//file:noinspection GroovyAssignabilityCheck
//file:noinspection DependencyNotationArgument

dependencies {
    // Minecraft - explicitly declare for main sourceset
    minecraft "com.mojang:minecraft:${project.minecraft_version}"
    mappings "net.fabricmc:yarn:${rootProject.yarn_mappings}:v2"

    // Core Forgero modules
    api(project(":modules:core"))
    api(project(path: ":modules:mc:loader", configuration: 'namedElements'))
    api(project(path: ":modules:mc:common", configuration: 'namedElements'))

    // Fabric - pattern from modules/mc/loader
    modApi("net.fabricmc.fabric-api:fabric-api:${project.fabric_api_version}")
    modApi("net.fabricmc:fabric-loader:${project.loader_version}")

    // JUnit 5
    api("org.junit.jupiter:junit-jupiter-api:5.9.0")
    testRuntimeOnly("org.junit.jupiter:junit-jupiter-engine:5.9.0")

    // Fabric Loader JUnit for GameTests
    testImplementation("net.fabricmc:fabric-loader-junit:${project.loader_version}")

    // Content data required for Forgero initialization in tests
    testRuntimeOnly(project(":content:minecraft-vanilla-materials"))
    testRuntimeOnly(project(":content:minecraft-tools"))
    testRuntimeOnly(project(":content:shared-materials"))
}

loom {
    runs {
        testServer {
            server()
            name = "Test Server"
            source sourceSets.test
        }
        gameTest {
            inherit testServer
            name "Game Test"
            vmArg "-Dfabric-api.gametest"
            runDir "build/gameTest"
        }
    }
}

sourceSets {
    test {
        compileClasspath += main.compileClasspath
        runtimeClasspath += main.runtimeClasspath
    }
}

test {
    useJUnitPlatform()
    testLogging.events("failed")
    testLogging.info.events = ["failed", "skipped"]
}

java {
    withSourcesJar()
}
