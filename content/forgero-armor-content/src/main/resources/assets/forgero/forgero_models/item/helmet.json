{
  "type": "forgero:equipment_model_template",
  "target": {
    "tag": "forgero:armor_material"
  },
  "models": [
    {
      "id": "forgero:item/{target.name}-helmet",
      "target": "forgero:{target.name}-helmet",
      "type": "forgero:texture_model",
      "layers": [
        {
          "order": 0,
          "template": "forgero:texture_template/helmet",
          "palette": "forgero:palette/{target.name}",
          "output": "forgero:item/{target.name}-helmet"
        }
      ]
    }
  ]
}
