package com.sigmundgranaas.forgero.core.attribute.api;

// Declares a components of something that should be combined and converted to an attribute.
// Marker interface for now, but should be expanded and configured to make it easier to deal with attribute components.
public sealed interface AttributeComponent permits CompositeAttributeComponent {
}
